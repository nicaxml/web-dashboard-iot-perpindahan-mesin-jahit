<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Data Mesin</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: "Arial", sans-serif;
    }

    body {
      background-color: #f9cf96;
      display: flex;
      min-height: 100vh;
      color: #000;
    }

    /* ===== SIDEBAR ===== */
    .sidebar {
      background-color: #fff2cc;
      width: 180px;
      padding: 20px 10px;
      display: flex;
      flex-direction: column;
      align-items: center;
      border-radius: 0 20px 20px 0;
      position: sticky;
      top: 0; /* sticky sidebar */
      height: 100vh;
    }

    /* Sapaan di atas sidebar */
    .sidebar .greeting {
      font-size: 16px;
      font-weight: 600;
      margin-bottom: 25px; /* beri jarak lebih */
      text-align: center;
    }

    .sidebar a {
      display: block;
      width: 85%;
      margin: 10px 0;
      padding: 8px 12px;
      text-align: center;
      text-decoration: none;
      color: #000;
      font-size: 16px;
      font-weight: 600;
      background-color: #c9d3ff;
      border-radius: 12px;
      box-shadow: 0 0 3px rgba(0,0,0,0.1);
      transition: all 0.3s;
      position: relative;
    }

    .sidebar a:hover {
      background-color: #aabaff;
      transform: scale(1.05);
    }

    .sidebar a.logout {
      background-color: #baff4d;
    }

    .sidebar a.logout:hover {
      background-color: #a3ff1f;
    }

    .sidebar a.active {
      border-left: 4px solid #3b3bff;
      background-color: #aabaff;
      transform: scale(1.05);
    }

    /* ===== MAIN CONTENT ===== */
    .main {
      flex: 1;
      display: flex;
      flex-direction: column;
      overflow: hidden;
      text-align: center;
    }

    /* ===== HEADER STICKY ===== */
    .header {
      background-color: #f9cf96;
      width: 100%;
      padding: 20px 0;
      text-align: center;
      position: sticky;
      top: 0;
      z-index: 100;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }

    .header h1 {
      font-size: 28px;
      font-weight: 800;
      margin-bottom: 5px;
    }

    .header h2 {
      font-size: 22px;
      font-weight: 700;
    }

    /* ===== TABLE ===== */
    .table-container {
      width: 95%;
      margin: 20px auto;
      height: calc(100vh - 140px); /* kurangi tinggi header */
      overflow-y: auto;
      overflow-x: auto;
    }

    table {
      width: 100%;
      min-width: 800px;
      border-collapse: collapse;
      text-align: center;
      background-color: #fff;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }

    th, td {
      padding: 10px;
      border: 1px solid #ddd;
    }

    th {
      background-color: #c9d3ff;
      color: #000;
      font-weight: bold;
      position: sticky;
      top: 0; /* sticky header tabel */
      z-index: 10;
    }

    tr:nth-child(even) {
      background-color: #f2f2f2;
    }

    tr:hover {
      background-color: #b8c3ff;
    }

    /* ===== RESPONSIVE ===== */
    @media (max-width: 768px) {
      body {
        flex-direction: column;
      }

      .sidebar {
        width: 100%;
        flex-direction: row;
        justify-content: space-around;
        border-radius: 0;
        padding: 10px;
        height: auto;
        position: relative;
      }

      .sidebar .greeting {
        display: none; /* sembunyikan di mobile */
      }

      .sidebar a {
        font-size: 14px;
        padding: 6px 8px;
        margin: 5px;
      }

      .header h1 {
        font-size: 22px;
      }

      .header h2 {
        font-size: 18px;
      }

      table {
        font-size: 12px;
      }
    }
  </style>
</head>
<body>

  <!-- SIDEBAR -->
  <div class="sidebar">
    <div class="greeting">Hi, Admin Sewing</div>
    <a href="/">Halaman Utama</a>
    <a href="/admin/dashboard">Dashboard</a>
    <a href="/admin/mesin" class="active">Mesin</a>
    <a href="/admin/riwayat">Riwayat</a>
    <a href="/logout" class="logout">Logout</a>
  </div>

  <!-- MAIN CONTENT -->
  <div class="main">
    <!-- HEADER STICKY -->
    <div class="header">
      <h1>DASHBOARD ADMIN</h1>
      <h1>SEWING MACHINE ACTIVITY</h1>
      <h2>Data Mesin</h2>
    </div>

    <!-- TABEL MESIN -->
    <div class="table-container">
      <table>
        <thead>
          <tr>
            <th>ID Mesin</th>
            <th>Merk Mesin</th>
            <th>Nomor Seri</th>
            <th>Kode Barcode</th>
            <th>Line Sekarang</th>
            <th>Line Sebelumnya</th>
            <th>Status Pindah</th>
            <th>Update Time</th>
          </tr>
        </thead>
        <tbody>
          <% mesin.forEach(m => { %>
            <tr>
              <td><%= m.id_mesin %></td>
              <td><%= m.merk_mesin %></td>
              <td><%= m.nomer_seri_mesin %></td>
              <td><%= m.kode_barcode || '-' %></td>
              <td><%= m.line_sekarang %></td>
              <td><%= m.line_sebelumnya || '-' %></td>
              <td><%= m.status_pindah %></td>
              <td><%= new Date(m.update_time).toLocaleString() %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>

</body>
</html>
